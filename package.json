{
  "name": "prisma-client-repro",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "postinstall": "prisma generate --schema=./prisma/schema.prisma && prisma generate --schema=./prisma/empty-schema.prisma"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "devDependencies": {
    "prisma": "^4.14.0-dev.21"
  },
  "dependencies": {
    "@prisma/client": "^4.14.0-dev.21",
    "@serverless-stack/cli": "1.18.4",
    "@serverless-stack/resources": "1.18.4",
    "aws-cdk-lib": "2.50.0"
  }
}
